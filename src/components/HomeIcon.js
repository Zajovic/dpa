import React from 'react';
import { Icon } from "@blueprintjs/core";

/*

    probaj ovakvo formatiranje, per-line, uvek je citljivije tako - ovo se odnosi na sve fajlove i komponente etc.
*/

const HomeIcon = () => {

    return (
        <Icon
            className="menuIcon"
            icon="menu"
            iconSize={20}
        />
    )

}

export default HomeIcon;